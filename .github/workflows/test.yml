name: Build & publish image
on:
  push: 
    tags:
      - v[0-9]+.[0-9]+.[0-9]+-rc[0-9]+
      - v[0-9]+.[0-9]+.[0-9]+
    branches:
      - '8-feature-support-for-multiple-service-deployment-from-same-repository'
  
jobs:
 build-and-push-dockerimages:
    strategy:
     fail-fast: false 
     matrix:
       project: [hrms_admin]
    permissions:
      contents: read
      packages: write
    secrets:
      GHCR_TOKEN: ${{ secrets.GHCR_TOKEN }}
      REPOSITORY_OWNER: ${{ secrets.REPOSITORY_OWNER }}  
    uses: ./.github/workflows/build.yml 
     
 deploy-to-kubernetes:
    needs: build-and-push-dockerimages
    strategy:
     fail-fast: false
     matrix:
       project: [hrms_admin, hrms_client]
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/deploy.yml 
    with:
      repository_name: ${{ github.event.repository.name }}
      version: ${{ needs.build-and-push-dockerimages.outputs.version }}
      release_name: ${{ matrix.project }}
      cluster_environment: ${{ needs.build-and-push-dockerimages.outputs.cluster_environment }}
    secrets:
      NAMESPACE: ${{ secrets.NAMESPACE }}
      JENKINS_URL: ${{ secrets.JENKINS_URL }}
      JENKINS_USER: ${{ secrets.JENKINS_USER }}
      JENKINS_TOKEN: ${{ secrets.JENKINS_TOKEN }}   
